<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ChitChat</title>
    <link rel="stylesheet" href="style.css">
    <link rel="icon" type="image" href="download.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <h1> ChitChats &#x1F60D </h1>
    
    <form id="btn1" method="GET" action="/chats/new">
        <button  class="btn btn-primary" type="submit">New Chat</button>
    </form>

    <br>

   <% for(let chat of chats){ %>
    <div class="main">
        <div class="chat">
            <p>From : <%= chat.from %> </p>
            <div class="msg">
                <p> Msg : <%= chat.message %> </p>
            </div>
            <div class="btn-cont">
            <form method="GET" action="/chats/<%=chat._id%>/edit">
                <button id="btn4">edit</button>
            </form>
            <form method="POST" action="/chats/<%=chat._id%>?_method=DELETE">
                <button id="btn4" onclick="btnfunc()">delete</button>
            </form>
            </div>
            <p> Received by : <%= chat.to %> </p>
            <p><%= chat.created.toString().split(" ")[4] %> </p>
            <p><%= chat.created.toString().split(" ").slice(0,4).join(" ") %> </p>
        </div>
        
    </div>
   <% } %>
</body>
</html>

